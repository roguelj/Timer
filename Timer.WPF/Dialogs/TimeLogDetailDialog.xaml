<UserControl x:Class="Timer.WPF.Dialogs.TimeLogDetailDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:converters="clr-namespace:Timer.WPF.Converters"
             xmlns:viewmodels="clr-namespace:Timer.WPF.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:TimeLogDetailViewModel}"
             mc:Ignorable="d" 
             d:Background="White"
             >


    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="True"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
        </Style>
    </prism:Dialog.WindowStyle>
    
    <UserControl.Resources>
        
        <Style x:Key="HeaderStyle" TargetType="Label">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="ControlStyle" TargetType="FrameworkElement">
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <CollectionViewSource Source="{Binding Tasks}" x:Key="TasksCollectionViewSource" Filter="CollectionViewSource_Filter"/>

        <converters:HtmlColourConverter x:Key="HtmlColourConverter" />
       
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Start & End times -->
        <Label Grid.Row="0" Grid.Column="0" Content="Start" Style="{StaticResource HeaderStyle}" />
        <xctk:DateTimePicker Grid.Row="0" Grid.Column="1" Value="{Binding StartDateTime}" Style="{StaticResource ControlStyle}" />
        <Label Grid.Row="1" Grid.Column="0" Content="End" Style="{StaticResource HeaderStyle}" />
        <xctk:DateTimePicker Grid.Row="1" Grid.Column="1" Value="{Binding EndDateTime}" Style="{StaticResource ControlStyle}"/>
        <Label Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Content="{Binding Duration,StringFormat=hh\\:mm}" />
                
        
        <!-- projects -->
        <Label Grid.Row="2" Grid.Column="0" Content="Project" Style="{StaticResource HeaderStyle}" />
        <ComboBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding Projects}" 
                  IsSynchronizedWithCurrentItem="True" 
                  SelectedItem="{Binding SelectedProject}" 
                  Style="{StaticResource ControlStyle}"
                  >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" />
                    </Grid>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <!-- tasks -->
        <Label Grid.Row="3" Grid.Column="0" Content="Task" Style="{StaticResource HeaderStyle}" />
        <ComboBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding Source={StaticResource TasksCollectionViewSource}}" 
                  IsSynchronizedWithCurrentItem="True" 
                  SelectedItem="{Binding SelectedTask}" 
                  Style="{StaticResource ControlStyle}"
                  x:Name="TaskComboBox">
            
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" />
                    </Grid>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <!-- tags -->
        <Label Grid.Row="4" Grid.Column="0" Content="Tags" Style="{StaticResource HeaderStyle}" />
        <ListBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" 
                 ItemsSource="{Binding Tags}" 
                 SelectionMode="Multiple" 
                 x:Name="SelectedTagsListBox" 
                 SelectionChanged="SelectedTagsListBox_SelectionChanged" 
                 Style="{StaticResource ControlStyle}">
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Name}" Background="{Binding Path=Colour, Converter={StaticResource HtmlColourConverter}}" Padding="1" Margin="1" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel MaxWidth="200"
                Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        
        <!-- Ok / Cancel etc-->
        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3">
            <Button Content="Ok" Command="{Binding CloseDialogOkCommand}" Style="{StaticResource ControlStyle}"/>
            <Button Content="Cancel" Command="{Binding CloseDialogCancelCommand}" Style="{StaticResource ControlStyle}"/>
        </StackPanel>

    </Grid>
    
</UserControl>
